<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- App theme for activities (NoActionBar so Compose can draw its own top bars) -->
    <style name="Theme.PromptNews" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Optional window tweaks -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>

        <!-- If you also use any view-system widgets, you can set colors here.
             Compose uses its own MaterialTheme in code, so this is mostly for window chrome. -->
    </style>

</resources>
